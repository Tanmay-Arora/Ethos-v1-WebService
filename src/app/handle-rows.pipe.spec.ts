import { HandleRowsPipe } from './handle-rows.pipe';

describe('HandleRowsPipe', () => {
  it('create an instance', () => {
    const pipe = new HandleRowsPipe();
    expect(pipe).toBeTruthy();
  });
});
